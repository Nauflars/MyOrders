services:
  # Nginx Web Server
  nginx:
    image: nginx:alpine
    container_name: myorders-nginx
    ports:
      - "80:80"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./public:/var/www/html/public:ro
      - ./var/log/nginx:/var/log/nginx
    depends_on:
      php:
        condition: service_healthy
    networks:
      - myorders-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  # PHP-FPM Application Server
  php:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: myorders-php
    volumes:
      - .:/var/www/html
      - ./var/cache:/var/www/html/var/cache
      - ./var/log:/var/www/html/var/log
    environment:
      - APP_ENV=${APP_ENV:-dev}
      - APP_SECRET=${APP_SECRET:-change-this-secret}
      - DATABASE_URL=${DATABASE_URL}
      - MONGODB_URL=${MONGODB_URL}
      - MONGODB_URI=${MONGODB_URI}
      - MONGODB_DB=${MONGODB_DB}
      - MESSENGER_TRANSPORT_DSN=${MESSENGER_TRANSPORT_DSN}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - REDIS_CACHE_DSN=${REDIS_CACHE_DSN:-redis://redis:6379/0}
      - REDIS_LOCK_DSN=${REDIS_LOCK_DSN:-redis://redis:6379/1}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_EMBEDDING_MODEL=${OPENAI_EMBEDDING_MODEL:-text-embedding-3-small}
    depends_on:
      mysql:
        condition: service_healthy
      mongodb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - myorders-network
    healthcheck:
      test: ["CMD", "php", "-v"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  # Messenger Worker 1 (High Priority Queue)
  worker1:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: myorders-worker-high
    command: php bin/console messenger:consume async_priority_high --time-limit=3600 --memory-limit=512M --limit=100 -vv
    volumes:
      - .:/var/www/html
      - ./var/cache:/var/www/html/var/cache
      - ./var/log:/var/www/html/var/log
    environment:
      - APP_ENV=${APP_ENV:-dev}
      - APP_SECRET=${APP_SECRET:-change-this-secret}
      - DATABASE_URL=${DATABASE_URL}
      - MONGODB_URL=${MONGODB_URL}
      - MONGODB_URI=${MONGODB_URI}
      - MONGODB_DB=${MONGODB_DB}
      - MESSENGER_TRANSPORT_DSN=${MESSENGER_TRANSPORT_DSN}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - REDIS_CACHE_DSN=${REDIS_CACHE_DSN:-redis://redis:6379/0}
      - REDIS_LOCK_DSN=${REDIS_LOCK_DSN:-redis://redis:6379/1}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    depends_on:
      mysql:
        condition: service_healthy
      mongodb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - myorders-network
    restart: unless-stopped

  # Messenger Worker 2 (Normal Priority Queue)
  worker2:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: myorders-worker-normal
    command: php bin/console messenger:consume async_priority_normal --time-limit=3600 --memory-limit=512M --limit=100 -vv
    volumes:
      - .:/var/www/html
      - ./var/cache:/var/www/html/var/cache
      - ./var/log:/var/www/html/var/log
    environment:
      - APP_ENV=${APP_ENV:-dev}
      - APP_SECRET=${APP_SECRET:-change-this-secret}
      - DATABASE_URL=${DATABASE_URL}
      - MONGODB_URL=${MONGODB_URL}
      - MONGODB_URI=${MONGODB_URI}
      - MONGODB_DB=${MONGODB_DB}
      - MESSENGER_TRANSPORT_DSN=${MESSENGER_TRANSPORT_DSN}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - REDIS_CACHE_DSN=${REDIS_CACHE_DSN:-redis://redis:6379/0}
      - REDIS_LOCK_DSN=${REDIS_LOCK_DSN:-redis://redis:6379/1}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    depends_on:
      mysql:
        condition: service_healthy
      mongodb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - myorders-network
    restart: unless-stopped

  # Messenger Worker 3 (Low Priority Queue)
  worker3:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: myorders-worker-low
    command: php bin/console messenger:consume async_priority_low --time-limit=3600 --memory-limit=512M --limit=100 -vv
    volumes:
      - .:/var/www/html
      - ./var/cache:/var/www/html/var/cache
      - ./var/log:/var/www/html/var/log
    environment:
      - APP_ENV=${APP_ENV:-dev}
      - APP_SECRET=${APP_SECRET:-change-this-secret}
      - DATABASE_URL=${DATABASE_URL}
      - MONGODB_URL=${MONGODB_URL}
      - MONGODB_URI=${MONGODB_URI}
      - MONGODB_DB=${MONGODB_DB}
      - MESSENGER_TRANSPORT_DSN=${MESSENGER_TRANSPORT_DSN}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - REDIS_CACHE_DSN=${REDIS_CACHE_DSN:-redis://redis:6379/0}
      - REDIS_LOCK_DSN=${REDIS_LOCK_DSN:-redis://redis:6379/1}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    depends_on:
      mysql:
        condition: service_healthy
      mongodb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - myorders-network
    restart: unless-stopped

  # Redis - Distributed Locking & Caching
  redis:
    image: redis:7-alpine
    container_name: myorders-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    networks:
      - myorders-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # MySQL Database (Source of Truth)
  mysql:
    image: mysql:8.0
    container_name: myorders-mysql
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD:-root_password}
      - MYSQL_DATABASE=${MYSQL_DATABASE:-myorders_db}
      - MYSQL_USER=${MYSQL_USER:-myorders_user}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD:-myorders_password}
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - myorders-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-root_password}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # MongoDB Database (Read Models & Embeddings)
  mongodb:
    image: mongo:7.0
    container_name: myorders-mongodb
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME:-myorders_user}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD:-myorders_password}
      - MONGO_INITDB_DATABASE=${MONGO_INITDB_DATABASE:-myorders}
    volumes:
      - mongodb-data:/data/db
      - mongodb-config:/data/configdb
    networks:
      - myorders-network
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # RabbitMQ Message Broker (CQRS & Async Processing)
  rabbitmq:
    image: rabbitmq:3-management
    container_name: myorders-rabbitmq
    ports:
      - "5672:5672"    # AMQP protocol
      - "15672:15672"  # Management UI
    environment:
      - RABBITMQ_DEFAULT_USER=${RABBITMQ_DEFAULT_USER:-guest}
      - RABBITMQ_DEFAULT_PASS=${RABBITMQ_DEFAULT_PASS:-guest}
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    networks:
      - myorders-network
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_port_connectivity"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # phpMyAdmin - MySQL Database Management
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: myorders-phpmyadmin
    ports:
      - "8080:80"
    environment:
      - PMA_HOST=mysql
      - PMA_PORT=3306
      - PMA_USER=${MYSQL_USER:-myorders_user}
      - PMA_PASSWORD=${MYSQL_PASSWORD:-myorders_password}
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD:-root_password}
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - myorders-network

  # mongo-express - MongoDB Database Management
  mongo-express:
    image: mongo-express:latest
    container_name: myorders-mongo-express
    ports:
      - "8081:8081"
    environment:
      - ME_CONFIG_MONGODB_ADMINUSERNAME=${MONGO_INITDB_ROOT_USERNAME:-myorders_user}
      - ME_CONFIG_MONGODB_ADMINPASSWORD=${MONGO_INITDB_ROOT_PASSWORD:-myorders_password}
      - ME_CONFIG_MONGODB_URL=mongodb://${MONGO_INITDB_ROOT_USERNAME:-myorders_user}:${MONGO_INITDB_ROOT_PASSWORD:-myorders_password}@mongodb:27017/
      - ME_CONFIG_BASICAUTH_USERNAME=${MONGO_EXPRESS_USER:-admin}
      - ME_CONFIG_BASICAUTH_PASSWORD=${MONGO_EXPRESS_PASSWORD:-admin}
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - myorders-network

# Persistent Volumes
volumes:
  mysql-data:
    driver: local
  mongodb-data:
    driver: local
  mongodb-config:
    driver: local
  rabbitmq-data:
    driver: local
  redis-data:
    driver: local

# Custom Network
networks:
  myorders-network:
    driver: bridge
